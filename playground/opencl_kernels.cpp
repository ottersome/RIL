#include <iostream>
#include <vector>
#define SIZE 100

int main(){

    std::vector<float> h_a(SIZE), h_b(SIZE), h_c(SIZE);

    //Populate a and b 
    for(size_t i = 0;i<SIZE;i++){
        h_a[i] = i;
        h_b[i] = 200;
    }



}
